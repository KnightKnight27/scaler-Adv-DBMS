Clock Sweep Cache Lab

This implementation demonstrates a simple cache eviction mechanism inspired by the clock sweep strategy.
The program inserts values into the cache, accesses them multiple times, and periodically removes entries whose usage count reaches zero.
