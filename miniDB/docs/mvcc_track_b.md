# Track B: MVCC

Planned after M4. The MVCC extension will add transaction timestamps, snapshot visibility, version chains, and write-write conflict detection, then compare against the required 2PL baseline.
